/**
 * Built-in default rule configuration.
 *
 * Extend it with `"extends": "npm-package-json-lint:default"` in a
 * .npmpackagejsonlintrc.json file.
 */
// eslint-disable-next-line @typescript-eslint/no-require-imports
export const defaultConfig = require('./default-rules.json');
